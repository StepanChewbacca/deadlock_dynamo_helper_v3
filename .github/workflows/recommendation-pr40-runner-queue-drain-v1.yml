name: Recommendation PR40 Runner Queue Drain V1

on:
  pull_request:
    paths:
      - '.github/workflows/recommendation-pr40-runner-queue-drain-v1.yml'

permissions:
  contents: read

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'PR40 runner queue drain is disabled.'
