name: Recommendation PR40 Runner Queue Drain V1

on:
  pull_request:
    paths:
      - '.github/workflows/recommendation-pr40-runner-queue-drain-v1.yml'

permissions:
  contents: read

jobs:
  drain:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Runner queue drain paused
        run: echo 'Runner queue drain is paused after the emergency cleanup window.'
