name: Recommendation Behavioral V5.2 Refinement Budgeted

on:
  pull_request:
    paths:
      - '.github/training-requests/recommendation-behavioral-v5-2-refinement-v1.json'

permissions:
  contents: read

concurrency:
  group: recommendation-behavioral-v5-2-refinement-v1
  cancel-in-progress: true

jobs:
  refinement:
    runs-on: self-hosted
    timeout-minutes: 30
    steps:
      - name: Checkout requested implementation
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js for preflight
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate authorization, lineage, and cost budget
        id: preflight
        shell: bash
        run: |
          set -euo pipefail
          request='.github/training-requests/recommendation-behavioral-v5-2-refinement-v1.json'
          test -s "$request"
          jq -e '
            .schemaVersion == 1 and
            .operation == "RECOMMENDATION_BEHAVIORAL_V5_2_BOUNDED_REFINEMENT" and
            .boundedRefinementAuthorized == true and
            .maximumTrainingMinutes == 20 and
            .fullTrainingAuthorized == false and
            .valueV8TrainingAuthorized == false and
            .productionRankingChanged == false and
            .passiveShadowAuthorized == false and
            .randomizedCanaryAuthorized == false and
            .sourceDirectory == "recommendation-pro-decision-dataset-v6-terminal-5000-v2" and
            .sampleDirectory == "recommendation-behavioral-v5-1-sweep-b-v1" and
            .sourceSweepDirectory == "recommendation-behavioral-v5-2-bounded-v3" and
            .outputDirectory == "recommendation-behavioral-v5-2-refinement-v1" and
            (.variants | length) == 3 and
            .common.foldCount == 5 and
            .common.l2 == 0.0001 and
            .common.supportProbability == 0.01 and
            .common.majorGroupMinDecisions == 100
          ' "$request"
          node --check scripts/run-recommendation-behavioral-v5-2-refinement.mjs

          storage='/var/lib/docker/volumes/deadlock_dynamo_helper_deadlock-storage/_data'
          source_name="$(jq -r '.sourceDirectory' "$request")"
          sample_name="$(jq -r '.sampleDirectory' "$request")"
          sweep_name="$(jq -r '.sourceSweepDirectory' "$request")"
          output_name="$(jq -r '.outputDirectory' "$request")"
          source="$storage/$source_name"
          sample="$storage/$sample_name/diagnostic-sample.ndjson.gz"
          sweep="$storage/$sweep_name/sweep-summary.json"
          output="$storage/$output_name"
          dataset_sha="$(jq -r '.expectedDatasetSha256' "$request")"
          sample_sha="$(jq -r '.expectedDiagnosticSampleSha256' "$request")"
          sweep_sha="$(jq -r '.expectedSourceSweepSummarySha256' "$request")"
          [[ "$dataset_sha" =~ ^[a-f0-9]{64}$ ]]
          [[ "$sample_sha" =~ ^[a-f0-9]{64}$ ]]
          [[ "$sweep_sha" =~ ^[a-f0-9]{64}$ ]]

          sudo test -s "$source/manifest.json"
          sudo test -s "$source/audit.json"
          sudo test -s "$sample"
          sudo test -s "$sweep"
          test "$(sudo sha256sum "$source/$(sudo jq -r '.artifact.fileName' "$source/manifest.json")" | awk '{print $1}')" = "$dataset_sha"
          test "$(sudo sha256sum "$sample" | awk '{print $1}')" = "$sample_sha"
          test "$(sudo sha256sum "$sweep" | awk '{print $1}')" = "$sweep_sha"
          sudo jq -e '.architectureContinuationRecommended == true and .fullTrainingAuthorized == false and .valueV8TrainingAuthorized == false' "$sweep"

          if sudo test -e "$output"; then
            echo "Refusing destructive reuse of existing output: $output" >&2
            exit 1
          fi
          sudo mkdir "$output"
          evidence_dir="$RUNNER_TEMP/recommendation-behavioral-v5-2-refinement-$GITHUB_RUN_ID"
          mkdir -p "$evidence_dir"
          {
            echo "storage=$storage"
            echo "source_name=$source_name"
            echo "sample_name=$sample_name"
            echo "sweep_name=$sweep_name"
            echo "output_name=$output_name"
            echo "dataset_sha=$dataset_sha"
            echo "sample_sha=$sample_sha"
            echo "sweep_sha=$sweep_sha"
            echo "evidence_dir=$evidence_dir"
          } >> "$GITHUB_OUTPUT"

      - name: Build isolated refinement image
        shell: bash
        run: |
          set -euo pipefail
          sudo docker build \
            --label deadlock.recommendation.operation=behavioral-v5-2-refinement \
            --label deadlock.recommendation.git-sha="$GITHUB_SHA" \
            -t "deadlock-behavioral-v5-2-refinement:$GITHUB_RUN_ID" \
            .

      - name: Run refinement with hard 20-minute training budget
        id: training
        shell: bash
        run: |
          set -uo pipefail
          storage='${{ steps.preflight.outputs.storage }}'
          source_name='${{ steps.preflight.outputs.source_name }}'
          sample_name='${{ steps.preflight.outputs.sample_name }}'
          sweep_name='${{ steps.preflight.outputs.sweep_name }}'
          output_name='${{ steps.preflight.outputs.output_name }}'
          evidence_dir='${{ steps.preflight.outputs.evidence_dir }}'
          container_name="deadlock-behavioral-v5-2-refinement-$GITHUB_RUN_ID"
          image="deadlock-behavioral-v5-2-refinement:$GITHUB_RUN_ID"

          set +e
          timeout --signal=TERM --kill-after=60s 20m sudo docker run --rm \
            --name "$container_name" \
            --stop-timeout 15 \
            --network none \
            --read-only \
            --tmpfs /tmp:rw,nosuid,nodev,size=512m \
            -e NODE_OPTIONS=--max-old-space-size=6144 \
            -e BEHAVIORAL_V5_2_REFINEMENT_REQUEST_PATH='/app/.github/training-requests/recommendation-behavioral-v5-2-refinement-v1.json' \
            -e DEADLOCK_RECOMMENDATION_BEHAVIORAL_V5_2_SOURCE_DIR="/app/apps/api/storage/$source_name" \
            -e BEHAVIORAL_V5_2_SAMPLE_PATH="/app/apps/api/storage/$sample_name/diagnostic-sample.ndjson.gz" \
            -e BEHAVIORAL_V5_2_SOURCE_SWEEP_SUMMARY_PATH="/app/apps/api/storage/$sweep_name/sweep-summary.json" \
            -e BEHAVIORAL_V5_2_REFINEMENT_OUTPUT_DIR="/app/apps/api/storage/$output_name" \
            -e EXPECTED_DATASET_SHA256='${{ steps.preflight.outputs.dataset_sha }}' \
            -e EXPECTED_DIAGNOSTIC_SAMPLE_SHA256='${{ steps.preflight.outputs.sample_sha }}' \
            -e EXPECTED_SOURCE_SWEEP_SUMMARY_SHA256='${{ steps.preflight.outputs.sweep_sha }}' \
            -v "$storage/$source_name:/app/apps/api/storage/$source_name:ro" \
            -v "$storage/$sample_name:/app/apps/api/storage/$sample_name:ro" \
            -v "$storage/$sweep_name:/app/apps/api/storage/$sweep_name:ro" \
            -v "$storage/$output_name:/app/apps/api/storage/$output_name:rw" \
            "$image" node /app/scripts/run-recommendation-behavioral-v5-2-refinement.mjs \
            2>&1 | tee "$evidence_dir/container.log"
          training_exit="${PIPESTATUS[0]}"
          set -e
          if [ "$training_exit" = '124' ] || [ "$training_exit" = '137' ]; then
            sudo docker stop --time 15 "$container_name" >/dev/null 2>&1 || true
            sudo docker rm -f "$container_name" >/dev/null 2>&1 || true
          fi
          echo "exit_code=$training_exit" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Verify refinement evidence
        shell: bash
        run: |
          set -euo pipefail
          test '${{ steps.training.outputs.exit_code }}' = '0'
          output='${{ steps.preflight.outputs.storage }}/${{ steps.preflight.outputs.output_name }}'
          evidence_dir='${{ steps.preflight.outputs.evidence_dir }}'
          sudo test -s "$output/refinement-summary.json"
          sudo jq -e '
            .operation == "RECOMMENDATION_BEHAVIORAL_V5_2_BOUNDED_REFINEMENT" and
            .source.futureTestRowCount == 0 and
            .refinementContract.onlyEmbeddingHashLatentLearningRateEpochsMayVary == true and
            .boundedRefinementAuthorized == true and
            .fullTrainingAuthorized == false and
            .valueV8TrainingAuthorized == false and
            .productionRankingChanged == false and
            .passiveShadowAuthorized == false and
            .randomizedCanaryAuthorized == false
          ' "$output/refinement-summary.json"
          sudo cp "$output/refinement-summary.json" "$evidence_dir/"
          sudo cp "$output/refinement-report.txt" "$evidence_dir/" 2>/dev/null || true
          sudo chown -R "$(id -u):$(id -g)" "$evidence_dir"

      - name: Upload refinement evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: recommendation-behavioral-v5-2-refinement-${{ github.run_id }}
          path: ${{ steps.preflight.outputs.evidence_dir }}
          if-no-files-found: warn
          retention-days: 7

      - name: Cleanup transient container and image
        if: always()
        shell: bash
        run: |
          sudo docker rm -f "deadlock-behavioral-v5-2-refinement-$GITHUB_RUN_ID" >/dev/null 2>&1 || true
          sudo docker image rm "deadlock-behavioral-v5-2-refinement:$GITHUB_RUN_ID" >/dev/null 2>&1 || true
